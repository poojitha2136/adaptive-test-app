/** @type {import('tailwindcss').Config} */
module.exports = {
  // Files Tailwind should scan for class names
  content: [
    "./src/**/*.{js,jsx,ts,tsx}",
  ],

  theme: {
    extend: {
      // Custom colors, fonts, spacing, or animations
      // can be added here for project-specific styling
    },
  },

  // Tailwind plugins (optional)
  plugins: [],
}
